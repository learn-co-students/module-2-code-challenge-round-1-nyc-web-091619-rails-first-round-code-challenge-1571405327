<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= link_to power.name.titlecase, power_path(power) %>
      <br>
      <%= power.description.capitalize %>
    </li>
    <br>
  <% end %>
</ul>
